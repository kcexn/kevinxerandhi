import { KeyvalueFilterPipe } from './keyvalue-filter.pipe';

describe('KeyvalueFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyvalueFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
